npm is a standard package manager for node.js
1) library of packages to use in npm 
2) command line tool ,install package ,update etc


when using import export 
when we import some function we cant directly run it we need to first change the typr to module in package.json

-require load complete js file with all the functions 
-import is new , it can selectively import the function need which is space efficent and async loading i done 

-library: use to controle small part of the code ,eg: AXIOS
-framework:complete app is define by these , eg : EXPRESS

-Express :
1)listen incomming request
2)parse to json
3)match resopnse with routes
4)send suitable resopnse
its in npm as package  
structure:{
require Express
app=express()
port;
app.listen(port,function)
}
 ctrl+c to stop the server 
-when request come this run .
-app.use=> to handle any routes
 app.use((req,res)=>{
    res.send(" " )-> can send obh , html , string anything 
    // it parses and send the response .
 })
-app.get("/routename",function) =>used for singel specific routename

-query Strings 
http://localhost:3000/search?q=hello for
app.get("/search", (req, res) => {
  console.log(req.query);
  res.send(" no result .");
});

req.params,req.query

--------------------------------------------
-EJS: embeded javascript language 

app.set("view engine",'ejs')
app.send()->send html, obj, string etc .
app.render()-> used to send big file 

--interpoltion syntax: embedding expression into marked up text .
<%= %>-> used to make operation work on page .

/:suryansh in link if taking data direct from index.js
/cats if taken from data base 

-> searving static files like style.css
app.use(express.static(path.join(__dirname, "public")));

-> include : used to make subtemplate : like for section like head in file called head.ejs in views .


-> get and post request 
get:- used to get some response , data sent in query string is limmited and is in string form only nad also it is visible in url 
post:- used to post something ,to create write update , any type of data can be sent and it is not displayed .
-for post request it cant directly read req.body so we use middleware:
=>app.use(express.urlencoded({ extended: true })); convert url encoded value to readable 
same for json in post to middleware:
app.use(express.json());

oops:
1) object prototype:mechanism by wgich javasc obj inherit feature from other objects.
like in a array we can difine diff function ,but other array will not get that  function 
because it is not in the prototype of array . 
-> arr.__proto__ -> this is the refrence to the prototype
changeing def of some method in prototype: arr.__proto__.push=(n)=>{
  console.log(n);
}
now when arr.push(5):->5 wil be printed 
-> accesing actual object : Array.prototype 

2) factory functions: function that create objects .
fucntion personamaker(name,age){
  const person={
    name: name ,
    age=age,
    talk(){
      console.log("hello i am bla bla bla .)
    }
  }
} this fucntion create persons object.

3) constructors: dosent return anything and start with capital letter
function Person(name,age){
  this.name=name;
  this.age=age;
  talk(){
    console.log("hello ji")
  }
}
let p1=new Person('Suryansh',21); here p1 is a instance  
new keyword used prototype concept .
and here p1.talk == p2.talk -> true , in factory function not .

4) class: 
class person{
  constructor(name,age){
    this.name=name;
    this.age=age;
  }talk(){
    console.log("hello ji")
  }
}

let p1=new Person("suryansh",21)

5)Inheritance:
class person{
  constructor(name,age){
    this.name=name;
    this.age=age;
  }talk(){
    console.log("hello ji")
  }
}

class sprotperson extends person{
  constructor(name,age,sport){
    super(name,age);
    this.sport=sport;
  }
}

-> REST:representational state transfer -> architectural style taht define set of constraints to be used for creating web services .
used for CRUD operations .(get,post,put(update existing data),patch(update existing data partially),delete)

-> redirect : used to redirect to diff url along with any response .
like when creating new post we are redirecting to te main age .

uuid-> universal unique id

-> Patch request-> 

in html only : get and post works 
we use method override 

Sql->use dbname
db.createCollection("students")
db.students.insertOne([{
  name:"Suryansh",
  age:21,
  city:"Nagpur"
}])
db.collectionname.operation()
operations-> find,findOne,update,updateOne,delete,deleteOne,replace etc.

db.collection.find({key:value})
db.collection.updateOne(
  {condition},
  {$set:{updatedValues}}
)
db.students.updateOne(
  {name:"Suryansh"},
  {$set:{age:22}}
)

db.students.replaceOne(
  {name:"Rahul"},
  {name:"Rahul", age:25}
)

db.students.deleteOne({name:"Rahul"})


mongoose->
const User(model)=mongoose.model("User"(collection),schemaname)
model name -> User save as user in small caps
-> mongoose use operation buffering : allow you to start using the model immediately ,without waiting for mongoose to establish a connection to monoDb.
